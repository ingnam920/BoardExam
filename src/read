*롬복 jdbapi sql-driver ~기타 스프링부트5초반 스프링이니셜라이저

*Get방식  (링크?)   //      Post방식

insert into user (email,name,password,regdate) values (?,?,?,now());
select LAST_INSERT_ID();
insert into user_role(user_id,role_id) values (?,1);
//회원가입정보 저장되고 권한정보도 저장했다.... (이3줄)
